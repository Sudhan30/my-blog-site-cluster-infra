# NOTE: This is a TEMPLATE file for reference only
# The actual grafana-credentials secret is created manually in the cluster
# DO NOT commit real passwords to Git
apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: web
type: Opaque
stringData:
  admin-password: ""  # Set this manually in cluster, not in Git
  admin-user: "admin"
