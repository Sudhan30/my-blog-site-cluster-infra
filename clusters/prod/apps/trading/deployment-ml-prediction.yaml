apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-prediction
  namespace: trading
  labels:
    app: ml-prediction
    app.kubernetes.io/part-of: algo-trading-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-prediction
  template:
    metadata:
      labels:
        app: ml-prediction
    spec:
      imagePullSecrets:
      - name: docker-hub-creds
      containers:
      - name: ml-prediction
        image: docker.io/sudhan03/algo-trading:ml-prediction-e6f56c7
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: PYTHONPATH
          value: "/app/src"
        - name: CONFIG_PATH
          value: "/app/config/config.yaml"
        - name: DB_HOST
          value: "timescaledb"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "trading_db"
        - name: DB_USER
          value: "trading_user"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: DB_PASSWORD
        volumeMounts:
        - name: config
          mountPath: /app/config
        - name: models
          mountPath: /app/models
        resources:
          requests:
            memory: "2Gi"
            cpu: "1000m"
          limits:
            memory: "4Gi"
            cpu: "4000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 60
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 30
      volumes:
      - name: config
        configMap:
          name: trading-config
      - name: models
        hostPath:
          path: /var/lib/rancher/k3s/storage/pvc-499f893f-7b25-4758-9cc5-4559b45c1ee0_orchestrator_models-pvc
          type: Directory
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: ml-prediction
  namespace: trading
  labels:
    app: ml-prediction
spec:
  selector:
    app: ml-prediction
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP
