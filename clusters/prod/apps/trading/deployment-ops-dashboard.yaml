---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ops-dashboard
  namespace: trading
  labels:
    app: ops-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ops-dashboard
  template:
    metadata:
      labels:
        app: ops-dashboard
    spec:
      imagePullSecrets:
      - name: docker-hub-creds
      containers:
      - name: ops-dashboard
        image: docker.io/sudhan03/algo-trading:ops-dashboard-a9dc6e9
        imagePullPolicy: Always
        ports:
        - containerPort: 8501
        env:
        - name: REDIS_HOST
          value: "redis"
        - name: DB_HOST
          value: "timescaledb"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: DB_PASSWORD
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: REDIS_PASSWORD
        - name: DASHBOARD_USERNAME
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: DASHBOARD_USERNAME
        - name: DASHBOARD_PASSWORD_HASH
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: DASHBOARD_PASSWORD_HASH
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: trading-secrets
              key: SESSION_SECRET
        volumeMounts:
        - name: config
          mountPath: /app/config
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
      volumes:
      - name: config
        configMap:
          name: trading-config
---
apiVersion: v1
kind: Service
metadata:
  name: ops-dashboard
  namespace: trading
spec:
  selector:
    app: ops-dashboard
  ports:
  - port: 8501
    targetPort: 8501
  type: ClusterIP
